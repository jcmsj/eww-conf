(defwidget music []
  (box :class {music == "" ? "":"music card"}
    :space-evenly false
    ;TODO handle overflow
    {music == "" ? "":"💿 ${substring(music, 0, 25)}"}))

(deflisten music :initial ""
; "playerctl --follow metadata --format '{{ artist }} - {{ title }}' || true")
"playerctl --follow metadata --format '{{ title }}' || false")
